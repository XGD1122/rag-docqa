"""Prompt 模板集中管理"""

# ---- RAG 问答 ----

QA_PROMPT = """你是文档问答助手。严格基于下面的文档片段回答问题，不得使用外部知识。

规则:
- 答案必须能直接从文档中找到依据
- 文档中找不到答案时，直接说"文档中未找到相关信息"，不要猜测
- 回答简洁直接，先给结论再展开细节
- 引用具体数据时要准确，不要改动数字

## 文档片段

{context}

## 问题

{question}

## 回答"""

# ---- 引用匹配 ----
# (Citation 使用 embedding 相似度匹配, 不需要 prompt)

# ---- 反思自检 ----

CLAIM_EXTRACTION_PROMPT = """从以下回答中提取所有关键事实声明。每个声明应该是一个独立的、可验证的事实陈述。

返回 JSON 格式:
{{"claims": ["声明1", "声明2", ...]}}

## 回答

{answer}

## 声明列表 (JSON)
"""

CLAIM_VERIFICATION_PROMPT = """判断以下声明是否能被提供的证据文档内容所支持。

声明: {claim}

证据内容:
{evidence}

请判断: 该声明是否被证据充分支持?
返回 JSON 格式:
{{"supported": true/false, "reason": "判断理由"}}
"""

FOLLOWUP_QUERY_PROMPT = """以下声明在当前文档中找不到证据支持。请生成 1-3 个补充检索查询, 用于在文档中重新搜索相关信息。

未支持的声明:
{unsupported_claims}

返回 JSON 格式:
{{"queries": ["查询1", "查询2", ...]}}
"""

# ---- Agent 工具 ----

TOOL_SYSTEM_PROMPT = """你是一个智能文档检索助手。你可以使用以下工具来处理用户的查询:

1. **search** — 在文档中搜索特定事实或信息 (适用于精确查找)
2. **compare** — 比较多份文档中关于某个主题的不同说法 (适用于对比分析)
3. **summarize** — 对某个主题在文档中的相关内容做摘要 (适用于概括性问题)
4. **extract_table** — 从文档中提取表格数据并格式化输出 (适用于表格类问题)
5. **ask_clarification** — 当用户问题不够明确时, 向用户询问澄清

请根据用户的问题, 选择合适的工具来回答。先用 search 检索相关内容, 然后基于检索结果给出最终答案。"""

COMPARE_PROMPT = """请根据以下文档内容, 对比分析用户的问题。

## 检索到的相关内容

{context}

## 对比问题

{question}

请从多个文档的角度进行对比分析, 指出异同点。
"""

SUMMARIZE_PROMPT = """请根据以下文档内容, 对用户的问题进行总结概括。

## 相关内容

{context}

## 问题

{question}

请给出全面但简洁的总结。
"""

TABLE_EXTRACTION_PROMPT = """请从以下文档内容中提取表格信息。

## 相关内容

{context}

## 表格相关问题

{question}

请以 Markdown 表格格式呈现提取的信息。如果文档中没有相关表格数据, 请说明。
"""
